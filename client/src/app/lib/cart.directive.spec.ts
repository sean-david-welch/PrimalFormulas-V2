import { CartDirective } from './cart.directive';

describe('CartDirective', () => {
  it('should create an instance', () => {
    const directive = new CartDirective();
    expect(directive).toBeTruthy();
  });
});
